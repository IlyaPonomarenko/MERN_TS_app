import { User } from "../models/user";

interface LoginProps {
    onDismiss: () => void
    onLogInSuccess: (user: User) => void
}

const LogIn = () => {
    return (
        <div>
            
        </div>
    );
};

export default LogIn;