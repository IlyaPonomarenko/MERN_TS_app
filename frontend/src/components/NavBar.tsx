import { User } from "../models/user";

interface NavBarProps{
    loggedInUser: User | null,
    
}

const NavBar = () => {
    return (
        <div>
            
        </div>
    );
};

export default NavBar;